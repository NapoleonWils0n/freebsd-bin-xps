#!/bin/sh

#===============================================================================
# flush-pipe
# Flush named pipe without blocking for OBS Studio media source
#===============================================================================

if [ -p /tmp/relay ]; then
    # Read everything currently in the pipe buffer and discard it
    dd if=/tmp/relay of=/dev/null iflag=nonblock status=none 2>/dev/null || true
fi
